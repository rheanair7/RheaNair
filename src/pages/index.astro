---
import { getCollection } from 'astro:content';
// Component Imports
import CallToAction from '../components/CallToAction.astro';
// Page section components
import ContactCTA from '../components/ContactCTA.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';
// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from '../layouts/BaseLayout.astro';

// Content Fetching: List four most recent work projects
const projects = (await getCollection('work'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);

// Content Fetching: List recent news and accomplishments  
const newsItems = (await getCollection('news'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 5);

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/
---

<BaseLayout>
	<div class="stack gap-20 lg:gap-48">
		<div class="wrapper stack gap-8 lg:gap-20">
			<header class="hero">
				<Hero
					title="Hello, my name is Rhea Nair"
					tagline="I am a Data Scientist who is currently based in Boulder, Colorado."
					align="start"
				>
					<div class="roles">
						<CallToAction href={import.meta.env.BASE_URL + "about/"} class="role-pill">
							<Icon icon="code" size="0.9em" /> Developer
						</CallToAction>
						<CallToAction href={import.meta.env.BASE_URL + "about/"} class="role-pill">
							<Icon icon="microphone-stage" size="0.9em" /> Speaker
						</CallToAction>
						<CallToAction href={import.meta.env.BASE_URL + "about/"} class="role-pill">
							<Icon icon="pencil-line" size="0.9em" /> Writer
						</CallToAction>
					</div>
				</Hero>

				<img
					alt="Rhea Nair"
					width="480"
					height="620"
					src={`${import.meta.env.BASE_URL}assets/potrait.jpg`}
					
					class="object-contain w-full h-48 rounded-lg"
				/>
			</header>
		</div>

		<main class="wrapper stack gap-20 lg:gap-48">
			<section class="section with-background with-cta">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Selected Work</h3>
					<p>Take a look below at some of my featured work for clients from the past few years.</p>
				</header>

				<div class="gallery">
					<Grid variant="offset">
						{
							projects.map((project) => (
								<li>
									<PortfolioPreview project={project} />
								</li>
							))
						}
					</Grid>
				</div>

				<div class="cta">
					<CallToAction href={import.meta.env.BASE_URL + "work/"}>
						View All
						<Icon icon="arrow-right" size="1.2em" />
					</CallToAction>
				</div>
			</section>

			<section class="section with-background bg-variant">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Latest News & Accomplishments</h3>
					<p>
						Stay updated with my recent achievements, publications, speaking engagements, and 
						professional milestones in data science and technology.
					</p>
				</header>

				<div class="news-gallery">
					<div class="news-timeline">
						{
							newsItems.map((newsItem) => (
								<article class="news-item">
									<time class="news-date">
										{newsItem.data.publishDate.toLocaleDateString('en-US', {
											year: 'numeric',
											month: 'short',
											day: '2-digit'
										})}
									</time>
									<div class="news-content">
										<p class="news-description" set:html={newsItem.data.description} />
									</div>
								</article>
							))
						}
					</div>
				</div>
			</section>
		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.roles {
		display: none;
	}

	.hero img {
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: top;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 6fr 4fr;
			padding-inline: 2.5rem;
			gap: 3.75rem;
		}

		.roles {
			margin-top: 0.5rem;
			display: flex;
			gap: 0.5rem;
		}

		.hero img {
			aspect-ratio: 5/ 4;
			border-radius: 4.5rem;
			object-fit: cover;
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	.section-header h3 {
		font-size: var(--text-2xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.section-header h3 {
			font-size: var(--text-4xl);
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	/* Center align the entire news section */
	.section.bg-variant {
		display: flex;
		flex-direction: column;
		align-items: center;
		width: 100%;
	}

	.section.bg-variant .section-header {
		text-align: center;
	}

	.news-gallery {
		display: flex;
		justify-content: center;
		align-items: center;
		width: 100%;
		max-width: none;
		padding: 0 2rem;
	}

	.news-timeline {
		background: var(--gray-999_40);
		border: 1px solid var(--gray-800);
		border-radius: 1rem;
		padding: 2.5rem;
		box-shadow: var(--shadow-md);
		max-height: 500px;
		overflow-y: auto;
		overflow-x: hidden;
		width: 100%;
		max-width: 1000px;
		margin: 0 auto;
		scrollbar-width: thin;
		scrollbar-color: var(--accent-regular) var(--gray-800);
	}

	/* Custom scrollbar for webkit browsers */
	.news-timeline::-webkit-scrollbar {
		width: 10px;
	}

	.news-timeline::-webkit-scrollbar-track {
		background: var(--gray-800);
		border-radius: 5px;
	}

	.news-timeline::-webkit-scrollbar-thumb {
		background: var(--accent-regular);
		border-radius: 5px;
	}

	.news-timeline::-webkit-scrollbar-thumb:hover {
		background: var(--accent-text-over);
	}

	/* Force scrollbar to always show */
	.news-timeline::-webkit-scrollbar-thumb {
		background: var(--accent-regular);
		border-radius: 5px;
		min-height: 20px;
	}

	/* Custom scrollbar for webkit browsers */
	.news-timeline::-webkit-scrollbar {
		width: 8px;
	}

	.news-timeline::-webkit-scrollbar-track {
		background: var(--gray-800);
		border-radius: 4px;
	}

	.news-timeline::-webkit-scrollbar-thumb {
		background: var(--accent-regular);
		border-radius: 4px;
	}

	.news-timeline::-webkit-scrollbar-thumb:hover {
		background: var(--accent-text-over);
	}

	.news-item {
		display: flex;
		gap: 2rem;
		align-items: flex-start;
		padding-bottom: 2rem;
		border-bottom: 1px solid var(--gray-800);
	}

	.news-item:last-child {
		border-bottom: none;
		padding-bottom: 0;
	}

	.news-date {
		flex-shrink: 0;
		width: 120px;
		font-size: var(--text-sm);
		font-weight: 600;
		color: var(--gray-100);
		text-align: left;
	}

	.news-content {
		flex: 1;
	}

	.news-description {
		color: var(--gray-300);
		line-height: 1.6;
		margin: 0;
	}

	.news-description em {
		color: var(--accent-regular);
		font-style: italic;
	}

	@media (max-width: 50em) {
		.news-gallery {
			padding: 0 1rem;
		}

		.news-timeline {
			padding: 1.5rem;
			max-height: 400px;
		}

		.news-item {
			flex-direction: column;
			gap: 0.5rem;
			padding-bottom: 1.5rem;
		}

		.news-date {
			width: auto;
			font-size: var(--text-xs);
		}
	}

	/* Role pill styles - much smaller version */
	.roles :global(.role-pill) {
		padding: 0.1em 0.3em !important;
		font-size: 0.6rem !important;
		font-weight: 500;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}

	.roles :global(.role-pill:hover) {
		transform: translateY(-1px);
		box-shadow: var(--shadow-lg);
	}

	.roles {
		display: flex;
		flex-wrap: wrap;
		gap: 0.3rem;
		justify-content: flex-start;
	}

	@media (max-width: 50em) {
		.roles :global(.role-pill) {
			font-size: 0.55rem !important;
			padding: 0.08em 0.25em !important;
		}
	}
</style>
